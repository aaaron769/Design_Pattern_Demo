/**
 * we are implementing the JumpBehaviour
 * so that the a fighter looking this typ of
 * behaviour to use it.
 */
class LongJump implements JumpBehavior
{
    public void jump()
    {
        System.out.println("Long Jump");
    }
}
